---
"deepskill": patch
---

Added `argument-hint` to `ask-deepu`'s frontmatter, so typing `/ask-deepu` shows a greyed-out example prompt instead of a blank input — matching the pattern already used by mattpocock's own stateful entry-point skills (`loop-me`, `teach`, `handoff`).
