{
  "name": "deepskill",
  "version": "0.8.2",
  "description": "Ask Deepu: an end-to-end PM pipeline for OpManager Plus feature and enhancement work, as a Claude Code plugin.",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "changeset": "changeset",
    "version": "changeset version && node scripts/sync-plugin-version.mjs",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "@changesets/changelog-github": "^0.5.0"
  }
}
